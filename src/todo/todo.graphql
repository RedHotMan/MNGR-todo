type Todo {
  id: ID!
  name: String!
  finished: Boolean!
}

type Query {
  todos: [Todo]!
}

type Query {
  hello: String!
}

type Mutation {
  createTodo(name: String!): Todo
}

type Mutation {
  toggleFinishedTodo(id: ID!): Todo
}
